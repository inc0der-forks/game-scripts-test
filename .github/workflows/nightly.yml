name: Build Contents Nightly

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
      - uses: actions/setup-node@v2
        with:
          node-version: 14
      - run: |
          npm install
          npm install typescript
          npm run build

      - name: Setup enviroment variables
        run: |
         echo "TAG=${GITHUB_REF/refs\/tags\//}" >> $GITHUB_ENV
         echo "::set-output name=sha_short::$(git rev-parse --short HEAD)"
  
      - name: Commit changes
        working-directory: ./Content
        run: |
         git init
         git config --local user.name "inc0der"
         git config --local user.email "inc0der.dev@gmail.com"
         git add .
         git commit -m "Nightly: From commit sh ${{ steps.vars.outputs.sha_short }}"
         git remote add origin "https://inc0der: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@github.com/inc0der-forks/game-scripts-test"
         git push origin master:develop --force
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Content
          path: Content/

